//highlight.directive.spec.ts

import { CustomtDirective } from './custom.directive';

describe('CustomDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomtDirective();
    expect(directive).toBeTruthy();
  });
});
